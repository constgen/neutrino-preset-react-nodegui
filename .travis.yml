language: node_js
node_js:
  - "12.0"
install:
#   - npm install
  - npm install --only=production
cache:
  directories:
  - node_modules
script: 
#   - npm test
  - echo "npm test temporarily disabled"
notifications:
  email:
    on_pull_requests: false
    on_success: change
    on_failure: change
deploy:
  edge: true
  provider: npm
  api_token: "$NPM_TOKEN"
  cleanup: true
  access: public
  on:
    tags: true
    branch: master
